<?xml version="1.0" encoding="utf-8"?>
<!--item_result.xml-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="4dp"
        android:layout_marginTop="6dp"
        android:layout_marginEnd="4dp"
        android:layout_marginBottom="6dp"
        app:cardBackgroundColor="@android:color/white"
        app:cardCornerRadius="16dp"
        app:cardElevation="2dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

            <!-- Número y enunciado de la pregunta -->
            <TextView
                android:id="@+id/tvResultEnunciado"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@string/tv_resuelta_enunciado_pregunta"
                android:textColor="#2D2D2D"
                android:textSize="14sp"
                android:textStyle="bold"
                android:layout_marginBottom="12dp"
                android:lineSpacingExtra="2dp" />

            <!-- Respuesta correcta -->
            <androidx.cardview.widget.CardView
                android:id="@+id/cardRespuestaCorrecta"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginBottom="8dp"
                app:cardBackgroundColor="#E8F5E9"
                app:cardCornerRadius="8dp"
                app:cardElevation="0dp"
                app:strokeColor="#66BB6A"
                app:strokeWidth="1dp">

                <TextView
                    android:id="@+id/tvRespuestaCorrecta"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:padding="12dp"
                    android:text="@string/result_respuesta_correcta_estatica"
                    android:textColor="#2E7D32"
                    android:textSize="13sp" />

            </androidx.cardview.widget.CardView>

            <!-- Respuesta incorrecta del usuario (solo si falló) -->
            <androidx.cardview.widget.CardView
                android:id="@+id/cardRespuestaIncorrecta"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:visibility="gone"
                app:cardBackgroundColor="#FFEBEE"
                app:cardCornerRadius="8dp"
                app:cardElevation="0dp"
                app:strokeColor="#EF5350"
                app:strokeWidth="1dp">

                <TextView
                    android:id="@+id/tvRespuestaIncorrecta"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:padding="12dp"
                    android:text="@string/result_respuesta_incorrecta_estatica"
                    android:textColor="#C62828"
                    android:textSize="13sp" />

            </androidx.cardview.widget.CardView>

        </LinearLayout>

    </androidx.cardview.widget.CardView>

</LinearLayout>
